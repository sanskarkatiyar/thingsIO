apiVersion: apps/v1
#
# Create a deployment ( https://kubernetes.io/docs/concepts/workloads/controllers/deployment/ )
#
kind: Deployment
metadata:
  name: dashboard-deployment
spec:
  replicas: 1
  # The .spec.selector field defines how the Deployment finds which Pods to manage.
  # In this case, you simply select a label that is defined in the Pod template (app: blog).
  # However, more sophisticated selection rules are possible,
  # as long as the Pod template itself satisfies the rule.
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
      - name: dashboard
        image: "gcr.io/csci5253-288221/thingsio/dashboard:1.0"
        ports:
        - containerPort: 5000
        env:
          - name: FLASK_PORT
            value: "5000"
          - name: GET_HOSTS_FROM
            value: dns
          - name: REDIS_SERVICE_HOST
            value: redis
          - name: RABBITMQ_SERVICE_HOST
            value: rabbitmq
          - name: INFLUXDB_SERVICE_HOST
            value: redis


